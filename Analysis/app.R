#A place to take the results from our pilot study and plot them for committee members and others that are interested

library(shiny)

ui <- fluidPage("Pilot Results")

server <- function(input, output){}

shinyApp(ui = ui, server = server)
